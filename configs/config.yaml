model:
  arch: mini_gpt4rec_v2
  model_type: pretrain_vicuna
  llama_model: "/content/SigLLM/ckpt/llm/base"
  prompt_path: "/content/SigLLM/prompts/qformer_prompt_movie.txt"
  # Vicuna-v1.5 conversation template — wraps each raw question from
  # prompt_path with the system message + USER/ASSISTANT markers Vicuna was
  # instruction-tuned on. The label ("Yes" / "No") is concatenated right
  # after "ASSISTANT:".
  prompt_template: 'A chat between a curious user and an artificial intelligence assistant. The assistant gives helpful, detailed, and polite answers to the user''s questions. USER: {} ASSISTANT:'
  
  freeze_rec: True
  freeze_proj: False

  max_txt_len: 1024
  end_sym: "###"
  ans_type: "v2"

  proj_token_num: ${model.qformer_config.num_queries}

  rec_model: "MF"
  user_num: -100
  item_num: -100

  qformer_config:
    use_qformer: True
    qformer_ckpt: "${run.qformer_stage2.output_dir}${run.qformer_stage2.qformer_ckpt_out}" # Q-Former weights after Stage 2 generative pretraining
    llama_proj_ckpt: "${run.qformer_stage2.output_dir}${run.qformer_stage2.proj_ckpt_out}" # Projection weights after Stage 2 generative pretraining
    qformer_d_model: 768
    qformer_output_dim: 768 # Keep Q-Former compact; llama_proj maps this to the LLM hidden size.
    qformer_text_model_name: "bert-base-uncased"
    max_instruction_length: 48
    num_queries: 8
    num_heads: 8
    num_layers: 2

  rec_config:
    user_num: 839
    item_num: 3256
    embedding_size: 256    
    pretrained_path: "/content/SigLLM/ckpt/mf/mf_model.pth"

  ckpt: null

datasets:
  movie_ood:
    path: "/content/SigLLM/data/processed/ml-1m/"
    data_type: default
    build_info:
      storage: "/content/SigLLM/data/processed/ml-1m/"

run:
  task: rec_pretrain
  output_dir: "ckpt/qformer_stage3_llm_finetune"
  device: "cuda"
  seed: 42
  rec_baseline:
    save_file: ${model.rec_config.pretrained_path}
    need_train: True
    warm_or_cold: "warm"
    seed: 2025
    lr: 1e-3
    wd: 1e-4
    embedding_size: ${model.rec_config.embedding_size}
    epoch: 5000
    eval_epoch: 1
    patience: 100
    batch_size: 1024
  qformer_stage1:
    # BLIP-2 representation pretraining: ITC + ITM + ITG using the Q-Former's
    # own text branch (no external BERT). Item-item contrastive (w_ii) is a
    # SigLLM-specific addition for co-watch collaborative signal.
    seed: 2025
    batch_size: 256
    num_workers: 4
    embedding_size: ${model.rec_config.embedding_size}
    user_num: ${model.rec_config.user_num}
    item_num: ${model.rec_config.item_num}
    num_queries: ${model.qformer_config.num_queries}
    num_heads: ${model.qformer_config.num_heads}
    num_layers: ${model.qformer_config.num_layers}
    qformer_d_model: ${model.qformer_config.qformer_d_model}
    qformer_output_dim: ${model.qformer_config.qformer_output_dim}
    qformer_text_model_name: ${model.qformer_config.qformer_text_model_name}
    max_instruction_length: ${model.qformer_config.max_instruction_length}
    item_pair_window: 2
    max_item_item_pairs: null
    max_user_item_pairs: null
    include_user_item: False
    lr: 2e-4
    w_itc: 1.0
    w_itm: 1.0
    w_itg: 1.0
    w_ii: 1.0
    tau_itc: 0.07
    tau_ii: 0.05
    weight_decay: 1e-3
    debug_batch: True
    debug_batch_max_steps: 10
    early_stopping_patience: 10
    early_stopping_min_delta: 1e-4
    best_checkpoint_name: "qformer_stage1_best_full.pt"
    best_qformer_weights_name: "qformer_stage1_best_qformer.pth"
    log_epoch: 1
    epoch: 100
    pretrained_rec_path: ${model.rec_config.pretrained_path}
    freeze_rec: True
    output_dir: "/content/SigLLM/ckpt/qformer_stage1/"

  qformer_stage2:
    # BLIP-2-style generative pretraining: train Q-Former + linear projection
    # on next-token LM of item-text captions with the LLM fully frozen (no LoRA).
    seed: 2025
    batch_size: 32
    num_workers: 4
    embedding_size: ${model.rec_config.embedding_size}
    user_num: ${model.rec_config.user_num}
    item_num: ${model.rec_config.item_num}
    num_queries: ${model.qformer_config.num_queries}
    num_heads: ${model.qformer_config.num_heads}
    num_layers: ${model.qformer_config.num_layers}
    qformer_d_model: ${model.qformer_config.qformer_d_model}
    qformer_output_dim: ${model.qformer_config.qformer_output_dim}
    qformer_text_model_name: ${model.qformer_config.qformer_text_model_name}
    max_instruction_length: ${model.qformer_config.max_instruction_length}
    llama_model_name: ${model.llama_model}
    pretrained_rec_path: ${model.rec_config.pretrained_path}
    qformer_ckpt_in: "${run.qformer_stage1.output_dir}${run.qformer_stage1.best_qformer_weights_name}"
    qformer_ckpt_out: "qformer_stage2_best_qformer.pth"
    proj_ckpt_out: "qformer_stage2_best_proj.pth"
    output_dir: "/content/SigLLM/ckpt/qformer_stage2/"
    lr: 1e-4
    weight_decay: 1e-3
    epoch: 20
    log_epoch: 1
    max_caption_length: 64
    early_stopping_patience: 5
    early_stopping_min_delta: 1e-4

  # Optimization
  lr_sched: "linear_warmup_cosine_lr"
  init_lr: 1e-4
  min_lr: 8e-5
  warmup_lr: 1e-5
  weight_decay: 1e-3
  amp: True
  
  # Training Schedule
  max_epoch: 200
  iters_per_epoch: 400
  batch_size_train: 16
  batch_size_eval: 64
  num_workers: 4
  warmup_steps: 200
  
  # Workflow
  evaluate: False
  mode: "v2"
  train_splits: ["train"]
  valid_splits: ["valid"]
  test_splits: ["test", "valid"]
  resume_ckpt_path: null

  # Distributed
  distributed: True
  world_size: 1
  dist_url: "env://"
